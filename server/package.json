{
  "name": "msngr-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "migrate:local": "wrangler d1 migrations apply msngr --local",
    "migrate": "wrangler d1 migrations apply msngr --remote",
    "deploy": "wrangler d1 migrations apply msngr --remote && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
