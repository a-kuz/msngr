# Локальные переменные wrangler dev.
# Пуши уходят в dev-мок APNs (tools/apns-mock.mjs) вместо Apple.
APNS_HOST=http://localhost:9871
# Искусственная задержка (мс) перед fanout сообщений — по умолчанию выключена.
# DEV_WS_LATENCY_MS=120
