# Прогон: переход по цитате, просмотр файлов, копирование и вставка медиа (задачи #42, #45)

Стенд: временный симулятор iPhone 17 (создан и удалён в рамках прогона), юзер agent_rf1,
собеседник bobby11 заведён через POST /api/register, чат — POST /api/chats токеном из
session.json контейнера. Сервер: отдельный `wrangler dev` на :8796 с `--persist-to`
(юзерский :8787 не трогался).

| Сценарий | Действие | Результат | Скриншот |
|---|---|---|---|
| Цитата → оригинал | 20 сообщений, ответ на «Сообщение 1», лента у низа (оригинал за экраном), тап по цитате | Лента прыгнула к «Сообщение 1» | reply-quote-before.png, reply-jump-after.png |
| Подсветка оригинала | тот же тап | Баббл оригинала вспыхнул акцентным фоном (~0.6с) | reply-jump-highlight.png |
| Просмотр PDF | Вложение → Файл → dogovor.pdf, тап по баббл-файлу | QLPreviewController с отрендеренной страницей, «1 of 1», кнопка «Поделиться» | file-pdf-quicklook.png |
| Просмотр текста | zametka.txt, тап по бабблу | QuickLook показал содержимое (кириллица не поехала) | file-txt-quicklook.png |
| Копирование фото | Лонг-тап по фото-сообщению | В меню появился пункт «Копировать» (раньше только у текста) | copy-photo-menu.png |
| Пункт «Вставить» | После копирования — меню вложений | Пункт «Вставить» показан (в буфере картинка, поле пустое) | paste-attach-menu.png |
| Превью вложения | Тап «Вставить» | Миниатюра с крестиком над инпут-баром, кнопка отправки активна | paste-preview.png |
| Отправка вставленного | Тап по кнопке отправки | Ушло фото-сообщением (00:44) | paste-sent.png |
| Системный жест вставки | Лонг-тап по пустому полю → Paste | Картинка из буфера ушла во вложение, а не в текст | field-paste-preview.png |

Тесты: MsngrTests (ReplyJumpTests — поиск оригинала по серверному msgId и локальному
clientMsgId; FilePreviewNameTests — имя файла для QuickLook), MsngrKit `swift test` — зелёные.

Не проверено живьём: догрузка истории при переходе к оригиналу за пределами
загруженной страницы (в чате прогона вся история помещалась в первую страницу);
share-лист для типов, которые QuickLook не открывает.
