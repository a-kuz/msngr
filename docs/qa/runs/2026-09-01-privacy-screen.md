# Run: the privacy screen gathers the whole list, 2026-09-01

Settings → «Приватность» on a fresh user (own simulator, iOS 26.5), two
screenshots covering the full screen: `2026-09-01-privacy-screen-1.png`,
`2026-09-01-privacy-screen-2.png`.

What the screen holds, top to bottom: last seen (tier + exceptions), profile
photo and bio (tier + exceptions), who finds me by number (tier + exceptions),
who adds me to groups (tier + exceptions), who can call me (tier +
exceptions), read receipts (reciprocal, with the wording saying so), typing,
link previews (the local fetch toggle), and the default disappearing timer
for new chats.

That is exactly the list the roadmap line asked to gather in one place; the
tiers themselves are each enforced by the server (their own runs and smoke
blocks). The line is done.
