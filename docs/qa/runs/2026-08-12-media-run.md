# Прогон медиа-кейсов, 2026-08-13

Стенд: wrangler dev :8787; dev iPhone 17 (44CE2242…, bobby11) ↔ iPhone 17 Pro Max (0E0CF155…, 445566). Билд из DerivedData `Msngr-dxlgamzmtgxlbmbikmytwvtaoorb`, установлен на оба симулятора перед прогоном. Скриншоты: `docs/qa/runs/2026-08-12-media/`. Slow Animations проверены через меню Debug — выключены на обоих.

## Итоги

| Кейс | Статус | Доказательства |
|---|---|---|
| 26. Видео: отправка и воспроизведение | **FAIL** | dev-16-player-open.png, dev-17/18 (md5 идентичны), dev-19-player-c.png; pm-03/04/05-player (md5 идентичны) |
| Фуллскрин-просмотрщик поверх шапки | **PASS** | dev-16-player-open.png, pm-03-player.png, pm-08-photofull.png |
| 21. Фото: отправка и превью | **PASS с оговоркой** | dev-39-photosel.png, pm-07-photorecv.png, pm-08-photofull.png, dev-41-list-before.png |
| 32. Read receipts и счётчики | **PASS (обратное направление)** | pm-10-sent-ticks.png, dev-42-badge.png, pm-11-ticks-after.png, dev-44-badge-after.png |
| 42. Спам 10 сообщений с dev | **BLOCKED** | dev-36-input.png, dev-32-typed.png |
| 13. Одновременные реакции | **BLOCKED** | dev-35-chat-afterreinstall.png |

## Кейс 26 — видео (главный): FAIL

Отправка с dev: плюс → «Фото или видео» → выбрано тестовое видео (цветные полосы, 0:04) → галка. Сообщение ушло, ✓✓, превью в ленте чёткое (dev-13-player-t0.png).

Воспроизведение — не работает на обоих устройствах:

- **dev (отправитель):** тап по превью открывает фуллскрин-плеер: чёрный экран, перечёркнутая иконка play (dev-16-player-open.png). Три скриншота с интервалом 1.5 с побайтово идентичны (md5 6ed18674… у всех трёх) — кадры не меняются. Тап по экрану показал контролы AVPlayer: таймлайн `--:-- … --:--`, т.е. AVPlayerItem не загрузился вовсе (dev-19-player-c.png). Тап по кнопке Play закрыл плеер (белый кадр перехода dev-20, затем список dev-22).
- **Pro Max (получатель):** видео дошло, превью чёткое (pm-02-bottom.png). Тап по превью — тот же чёрный экран с перечёркнутым play, три кадра с интервалом 1.5 с идентичны (md5 17cccc91… у всех трёх): pm-03/04/05.

Итого: доставка и превью работают, декодирование/открытие файла плеером — нет. Симптом совпадает с классом бага из коммита 7d1e4d6 («AVPlayer can't open extensionless video/audio») — но это привязка по симптому, код не проверял.

Наблюдение рядом: в чате оказалось два одинаковых видео 12:56 и 12:58 при одном подтверждении отправки (dev-15-player-t2.png). В первой половине прогона на dev-симуляторе работали чужие агенты (см. «Помехи»), однозначно атрибутировать дубль не могу.

## Фуллскрин поверх шапки: PASS

Во всех открытиях просмотрщика (видео-плеер на dev и PM, фото-фуллскрин на PM) шапка чата (имя, аватар, «в сети») не видна — только крестик и share: dev-16-player-open.png, pm-03-player.png, pm-08-photofull.png.

## Кейс 21 — фото: PASS с оговоркой

dev → PM, фото цветов из галереи: пикер (dev-39-photosel.png) → отправка. У получателя превью чёткое, корректное (pm-07-photorecv.png), тап открывает фуллскрин с фото (pm-08-photofull.png). В списке чатов dev — «Фото», ✓✓ (dev-41-list-before.png).

Оговорки: превью у отправителя в ленте (blurhash → чёткое) проверить нельзя — лента чата на dev к этому моменту не рендерилась (см. «Критический баг»). Зум в фуллскрине не проверял.

## Кейс 32 — read receipts: PASS, но в обратном направлении

Задумано dev→PM; ввод текста на dev мёртв (см. баг), поэтому роли поменяны: отправитель PM, получатель dev.

1. PM отправил текст, чат на dev закрыт: у PM серые ✓✓ (pm-10-sent-ticks.png), на dev в списке бейдж «1» и превью обновилось (dev-42-badge.png).
2. Открыл чат на dev: у PM галочки стали зелёными ✓✓ (pm-11-ticks-after.png), бейдж на dev погас (dev-44-badge-after.png).

Read receipt срабатывает даже при пустой (сломанной) ленте на dev. Исходное направление dev→PM не проверено.

## Кейсы 42 (спам) и 13 (реакции): BLOCKED

Оба требуют рабочего чата на dev: 42 — ввода текста, 13 — лонг-тапа по сообщению в ленте. После падения приложения (~13:05) лента чата на dev пуста и поле ввода не фокусируется (см. ниже), поэтому шаги выполнить нельзя. Не прогонял, не имитировал.

## Критический баг (найден по ходу): после перезапуска приложения чат открывается пустым, ввод мёртв

Воспроизведение на dev:

1. ~13:05 приложение на dev умерло (процесс исчез; совпало по времени с установкой на симулятор MsngrUITests-Runner чужим процессом — см. «Помехи», причинность не доказана).
2. После `simctl launch`: список чатов корректен, превью и бейджи живут (dev-34-reinstall.png), но чат 445566 открывается полностью пустым — ни одного сообщения (dev-27-chat5.png, dev-31-chat445566.png). Чат заявки Design Reviewer тоже пуст при бейдже «2» (dev-25-chat3.png).
3. Поле ввода не получает фокус: тап по полю не открывает клавиатуру, `idb ui text` не вводит ничего (dev-36-input.png). При этом плюс-меню и отправка вложений работают (dev-37-plus.png, фото ушло), read-помечание при открытии чата тоже работает.
4. Не лечится ни terminate+launch, ни переустановкой билда поверх (dev-35-chat-afterreinstall.png). Свайпы не помогают (dev-28-swipe.png). В логах приложения ошибок нет, API `/api/chats` отвечает 200.

Т.е. локальные данные на месте (список их видит), а лента и инпут в чате мертвы. Блокирует любую работу с историей на устройстве после перезапуска.

## Помехи среды (важно для трактовки)

- Первая половина прогона (до ~13:10) шла при активных чужих агентах на dev-симуляторе: посторонние тапы (сброс моего пикера, выбор не того элемента), чужие черновики/отправки «Anim test», «Anim test 2», «123123123», в 13:08 на dev ставился MsngrUITests-Runner (иконки на домашнем экране, dev-23-now.png). После сообщения оркестратора об эксклюзивности чужих тапов не наблюдал, испорченные сценарии перегнал заново (пикер, отправка видео/фото — чистые повторы).
- Всё время прогона на bobby11 приходили заявки/сообщения с чужих стендов (Design Reviewer, Palette QA) — на кейсы не влияли, но сдвигали строки списка.
- FAIL кейса 26 к помехам не свожу: чёрный плеер воспроизводится моими собственными тапами на обоих устройствах, после ухода чужих агентов в том числе (pm-03 — 13:23).
- Технический момент: idb-тапы на dev не доходили, пока не перезапустил idb_companion этого симулятора; на HID-ввод текста на PM влияет русская раскладка/автокоррекция (текст «rr test» превратился в «Векак Еве» — для кейса 32 содержание не важно).

## Что стоит сделать

1. Разобрать баг «пустая лента + мёртвый инпут после перезапуска» на dev (симулятор оставлен в этом состоянии — можно дебажить вживую).
2. Починить воспроизведение видео (кейс 26) и перегнать.
3. После фикса ленты перегнать 42, 13 и 32 в исходном направлении, а также сендерную часть 21 (blurhash → чёткое).
4. Проверить дубль видео (12:56/12:58) на чистом стенде без параллельных агентов.
